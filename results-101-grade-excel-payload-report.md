# Báo cáo chấm

Nguồn payload: `/home/ast/Workspace_ClaudeCode/chamAudit/results-101-grade-excel-payload.json`

## Tổng hợp lớp

- Điểm thô: **932.0/1920**
- Điểm tổng: **4.85/10**
- Câu có trả lời: **159/192**

## Xếp hạng học viên

| Hạng | Học viên | Điểm tổng /10 | Điểm thô | Câu có trả lời | Nhận xét tổng quan |
|---:|---|---:|---:|---:|---|
| 1 | AnhTP43 | 7.33 | 88.0/120 | 12/12 | Kiến thức tốt: nắm chắc SQL truy vấn cơ bản; tạo bảng + khai báo PRIMARY KEY/FOREIGN KEY đúng cấu trúc; viết chuẩn câu SELECT/JOIN/WHERE/UPDATE/ALTER TABLE đơn giản; hiểu nhanh đề coding easy/medium (3 câu đạt 10 điểm). Kiến thức cần cải thiện: cú pháp Stored Procedure (plpgsql) viết sai, code không chạy được ở cả 2 bài SP (dùng lẫn cú pháp nhiều hệ CSDL); truy vấn có aggregate còn vi phạm quy tắc GROUP BY (đưa cột ngoài nhóm vào SELECT); chưa quen kỹ thuật LEFT JOIN...IS NULL hoặc NOT IN/NOT EXISTS cho bài 'sản phẩm chưa từng bán'; hay nhầm cột điều kiện (WHERE MaSP thay vì MaLoaiSP) khiến thiếu WHERE đúng theo đề. Mức độ hoàn chỉnh: đã trả lời cả 12/12 câu, không bỏ trống; nhóm câu truy vấn/tạo bảng cơ bản làm tốt (3 câu 10 điểm, các câu khác chủ yếu 7-9 điểm), các câu aggregate/CTE/SP và logic UNION/LEFT JOIN còn yếu (4-6 điểm); chưa có câu Hard nào đạt phần nâng cao. Hướng cải thiện: (1) chọn và nắm chắc 1 hệ CSDL (PostgreSQL hoặc SQL Server), chạy thử SP/CTE trước khi nộp, không lẫn cú pháp; (2) ôn quy tắc GROUP BY khi SELECT cột ngoài nhóm và phân biệt rõ WHERE/ON; (3) luyện subquery NOT EXISTS/NOT IN, LEFT JOIN...IS NULL, CTE, xử lý NULL trong aggregate và TRY...CATCH cho SP; (4) đọc kỹ tên cột điều kiện trong đề trước khi viết WHERE (MaSP vs MaLoaiSP); (5) bổ sung UNIQUE/NOT NULL/CHECK khi đề yêu cầu ràng buộc và viết phần lý giải đi kèm để lấy trọn điểm nice-to-have. |
| 2 | NamMP2 | 7.00 | 84.0/120 | 12/12 | Kiến thức tốt: NamMP2 nắm chắc nền tảng SQL — xác định đúng khóa chính/khóa ngoại với bảng trung gian, hiểu rõ khái niệm trigger, alias, các phép join cơ bản, và viết chuẩn các truy vấn đơn giản như lọc theo điều kiện còn hoạt động của subquery/join và WHERE theo chuỗi. Kiến thức cần cải thiện: các bài Hard về tập hợp (liệt kê nhân viên chưa bán hàng hoặc chỉ bán OMO) còn sai logic; cú pháp procedure/trigger viết theo PostgreSQL lẫn lộn nhiều lỗi (OLD, NEW, RETURN, cờ I/D); quy ước trạng thái nhân viên (Status = 0 là đang làm) và cách chọn cột theo yêu cầu chưa tuân thủ đề. Mức độ hoàn chỉnh: 12/12 câu đều có câu trả lời, không câu nào bỏ trống; tuy nhiên mức độ đúng sai phân hóa — mạnh ở câu dễ/trung bình khái niệm, yếu ở bài Hard và cấu trúc hàm/trigger. Hướng cải thiện: luyện tập các mẫu truy vấn GROUP BY + HAVING và LEFT JOIN ... IS NULL cho tập hợp âm; viết thử trực tiếp trong PostgreSQL để bắt lỗi cú pháp trigger/UDF; đọc kỹ yêu cầu về trường trạng thái và danh sách cột cần select trước khi viết. |
| 3 | HoangNQ26 | 6.08 | 73.0/120 | 12/12 | Kiến thức tốt: học viên nắm chắc các thao tác SQL cơ bản (SELECT/INSERT, tạo bảng có khóa chính tự tăng, hàm với tham số đầu vào), hiểu rõ sự khác biệt giữa LIKE và =, và khái niệm trigger; kỹ năng xác định khóa chính/khóa ngoại từ lược đồ khá chính xác. Kiến thức cần cải thiện: cú pháp SQL nâng cao — trigger chưa hoàn chỉnh (thiếu FOR INSERT/UPDATE, bảng INSERTED, ROLLBACK), subquery và View không chạy được do GROUP BY sai cột, UDF sai cú pháp RETURNS/DECLARE; cũng hay bỏ sót ràng buộc NOT NULL/CHECK và tiền tố N cho chuỗi tiếng Việt. Mức độ hoàn chỉnh: 12/12 câu đều có trả lời, không có câu bỏ trống; tuy nhiên vài câu chỉ dừng ở khung sườn chưa chạy được và nhiều câu đạt must-have nhưng chưa chạm nice-to-have. Hướng cải thiện: viết và tự chạy thử từng câu lệnh trên một bộ CSDL mẫu để bảo đảm chạy được; ôn cấu trúc trigger SQL Server (INSERTED/DELETED, ROLLBACK, RAISERROR); bổ sung đủ ràng buộc NOT NULL/CHECK/DEFAULT và dùng mẫu chuỗi N'...' cho dữ liệu tiếng Việt. |
| 4 | DatTC6 | 5.58 | 67.0/120 | 10/12 | Kiến thức tốt: nắm chắc các khái niệm nền tảng (ORDER BY, khác biệt UNIQUE vs PRIMARY KEY, bản chất Stored Procedure vs Trigger), phân tích khóa chính/khóa ngoại khá chính xác và có tư duy đúng hướng cho các bài JOIN/tính tổng hợp. Kiến thức cần cải thiện: cú pháp DDL (ADD CONSTRAINT, khóa chính ghép, khóa ngoại), viết hoàn chỉnh View/UDF/CREATE TABLE, xử lý chuỗi tiếng Việt (dấu nháy đơn, tiền tố N) và chính tả tên bảng/cột. Mức độ hoàn chỉnh: 2 câu bỏ trống (DBA-H-14, DBA-H-23), 1 câu chỉ dở dang vài từ (DBA-M-29), nhiều câu đạt một phần must-have vì lỗi cú pháp nhỏ. Hướng cải thiện: chọn một hệ CSDL cụ thể (vd SQL Server), viết lại từng bài và chạy thử thật, luôn kèm INSERT dữ liệu test, rà soát chính tả tên bảng/cột trước khi nộp. |
| 5 | PhucNQ16 | 5.58 | 67.0/120 | 11/12 | Kiến thức tốt: nắm được subquery, các phép DML, điều kiện LIKE và xử lý chuỗi tiếng Việt; biết dùng NOT IN với subquery cho bài hai tập hợp. Kiến thức cần cải thiện: khái niệm NULL, trigger, cú pháp tạo ràng buộc FOREIGN KEY và thiết kế DDL đầy đủ. Mức độ hoàn chỉnh: phần lớn câu đạt must-have cơ bản nhưng nhiều câu bỏ qua yêu cầu phụ (danh sách nhân viên, Stored Procedure, đủ 3 bảng DDL); có 1 câu bỏ trống. Hướng cải thiện: đọc kỹ đề trả đủ các phần yêu cầu, luyện cú pháp DDL/ALTER TABLE chính xác, viết Stored Procedure có tham số và trình bày lý giải khi chọn kiểu dữ liệu. |
| 6 | KhoaNTM2 | 5.50 | 66.0/120 | 10/12 | Kiến thức tốt: nắm chắc các thao tác DDL/DML cơ bản (DROP COLUMN, INSERT, khái niệm GROUP BY, INSERT INTO) viết gọn nhưng chính xác; hiểu nguyên tắc tạo Stored Procedure có tham số và có thể triển khai ở mức cơ bản. Kiến thức cần cải thiện: cú pháp cụ thể của SQL Server (CREATE PROCEDURE/Function với tham số @, chuỗi N'...' có dấu tiếng Việt), hàm xử lý ngày (DATEDIFF so với trừ EXTRACT), và câu SQL phải chạy được với cú pháp hợp lệ. Mức độ hoàn chỉnh: 10/12 câu có nội dung, trong đó 2 câu đạt điểm tối đa (các thao tác đơn giản) nhưng có câu trả lời dở dang (chỉ 'SELECT') và 2 câu để trống — nhiều điểm bị mất vì bỏ dở giữa chừng. Hướng cải thiện: hoàn thiện câu trả lời trước khi nộp (câu SQL phải chạy được cú pháp), chọn một hệ CSDL (SQL Server hoặc PostgreSQL) và bám sát đúng loại cú pháp của hệ đó; tập trung thêm vào subquery/GROUP BY với HAVING, JOIN phức tạp và trigger/UDF. |
| 7 | NgocNLT2 | 5.50 | 66.0/120 | 9/12 | Kiến thức tốt: Hiểu rõ khái niệm cơ bản (PK/FK, Subquery, GROUP BY/DISTINCT, đếm theo nhóm) và viết truy vấn đơn giản chính xác. Kiến thức cần cải thiện: Logic JOIN nhiều bảng và ràng buộc của GROUP BY với các cột không tổng hợp (câu 6, 12); chuẩn hoá tên bảng/khóa và xử lý chuỗi tiếng Việt với N'...' (câu 9); triển khai Stored Procedure và thiết kế logic nâng cao (window function, top N với trùng lương) bị bỏ trống ở câu 5, 11, 13. Mức độ hoàn chỉnh: 9/12 câu được trả lời, nhưng chất lượng không đồng đều - một nửa đạt khá/tốt, một nửa lỗi logic hoặc thiếu phần quan trọng. Hướng cải thiện: rà soát kỹ logic LEFT JOIN vs INNER JOIN, GROUP BY đúng tập cột; bổ sung kỹ năng viết SP, DENSE_RANK/RANK với dữ liệu trùng; tránh để trống câu hỏi dù khó. |
| 8 | NhuanLD3 | 5.42 | 65.0/120 | 12/12 | Nhận xét: Học viên NhuanLD3 cho sheet NhuanLD3 thể hiện kiến thức tốt ở các thao tác SQL cơ bản (SELECT, ORDER BY, LIMIT/TOP, LEFT JOIN) và có hướng đi đúng ở câu conceptual về bảng (chạm ý entity) và trigger. Kiến thức cần cải thiện: cú pháp PostgreSQL (CREATE FUNCTION/RETURNS TRIGGER, RAISE EXCEPTION, GROUP BY/HAVING), đánh máy chính xác tên cột, và hiểu biết về FUNCTION/trigger nâng cao. Mức độ hoàn chỉnh: trung bình — vài câu đạt tốt (row 5, 6, 9), không câu nào bị bỏ trống, nhưng có những câu chỉ mới dáp kiểu cõi (row 2, 10) hoặc sai logic SQL đáng kể. Hướng cải thiện: cần viết SQL chạy được (không lỗi cú pháp, không alias sai), luyện kỹ CREATE FUNCTION/Trigger cú pháp chuẩn, và chú ý chọn kiểu dữ liệu tối ưu (INT vs BIGINT, DATE vs TIMESTAMP). |
| 9 | BaoVN5 | 5.00 | 60.0/120 | 12/12 | Kiến thức tốt: nắm vững khái niệm khóa ngoại và mục đích Stored Procedure, viết truy vấn cơ bản chính xác (Top 5 theo tuổi với ORDER BY + LIMIT, INSERT tách cột đúng thứ tự và kiểu dữ liệu), biết dùng khóa chính tự tăng khi tạo bảng. Kiến thức cần cải thiện: cú pháp chuẩn SQL Server (SELECT TOP/OFFSET FETCH thay LIMIT, IDENTITY thay AUTOINCREMENT, dấu nháy đơn cho chuỗi), cách viết Stored Procedure và View hoàn chỉnh với đúng cú pháp và tham số, logic GROUP BY/HAVING cho bài toán trùng lặp, LEFT JOIN ... IS NULL hoặc NOT EXISTS cho bài toán 'sản phẩm chưa từng bán', kỹ thuật CTE khi so sánh giá trị chi tiết với giá trị tổng hợp. Mức độ hoàn chỉnh: 3 câu đạt hoàn chỉnh (8-10 điểm), phần còn lại nhiều câu SQL dở dang (SELECT FROM ... WHERE / INNER JOIN / CREATE VIEW không tên) và bài tạo 4 bảng thiếu hẳn phần chèn dữ liệu dù đề yêu cầu, dẫn đến điểm số nhìn chung thấp. Hướng cải thiện: viết trọn vẹn từng câu lệnh từ đầu đến cuối trước khi nộp, không bỏ dở giữa truy vấn; khi đề yêu cầu chèn dữ liệu phải có thêm phần INSERT; rà soát lại ràng buộc và cú pháp DDL theo chuẩn SQL Server. |
| 10 | HaPN16 | 5.00 | 60.0/120 | 11/12 | Kiến thức tốt: nắm vững cú pháp SQL cơ bản (SELECT/UPDATE, JOIN, DISTINCT, rà soát khóa chính) và giải thích khái niệm ở mức cần thiết. Cần cải thiện: khái niệm nền tảng (Index, Stored Procedure), câu lệnh DDL nâng cao (Trigger, UDF), cẩn thận cú pháp FOREIGN KEY...REFERENCES và dấu phẩy thừa, làm trọn bài thay vì bỏ dở giữa chừng. Mức độ hoàn chỉnh: hoàn thành tốt các câu SQL cơ bản; nhiều câu nâng cao bị làm lệch yêu cầu (UDF chạy dưới dạng SELECT) hoặc dở dang (1/4 bảng), 3 câu gần như bỏ trống. Hướng cải thiện: ôn lại định nghĩa chuẩn của Index/StoredProcedure, luyện Trigger, UDF, subquery kết hợp GROUP BY/HAVING, đọc kỹ đề trước khi viết và luôn kiểm tra câu lệnh chạy được. |
| 11 | DuongTD9 | 4.67 | 56.0/120 | 8/12 | Kiến thức tốt: phân tích khóa chính/khóa ngoại chi tiết và đúng; viết JOIN đa bảng theo đúng quan hệ khóa ngoại; truy vấn SELECT-WHERE cơ bản chính xác và chọn kiểu dữ liệu hợp lý. Kiến thức cần cải thiện: cú pháp DDL (CREATE TABLE) còn sai cấu trúc; subquery-View và Stored Procedure/UDF còn rất yếu; hay bỏ sót DISTINCT, tên cột lệch lược đồ, chuỗi tiếng Việt thiếu dấu. Mức độ hoàn chỉnh: 3/12 câu bỏ trống (score 0) và 1 câu dở dang; các câu trả lời được phần lớn đạt must-have, chưa chạm optional. Hướng cải thiện: luyện viết CREATE TABLE chuẩn trong một khối và chèn dữ liệu; hoàn thiện subquery (GROUP BY + HAVING) và View; dùng chuỗi N'...' có dấu tiếng Việt; đọc kỹ lược đồ trước khi đặt tên cột. |
| 12 | TrungCT7 | 4.50 | 54.0/120 | 11/12 | Kiến thức tốt: nắm được khái niệm khóa chính/khóa ngoại, các phép join cơ bản và cú pháp UPDATE/SELECT đơn giản — câu dễ DBA-E-19 làm trọn 10 điểm. Kiến thức cần cải thiện: cú pháp SQL chưa chuẩn (CREATE VIEW thiếu AS, tên cột không khớp schema, dùng hàm không tồn tại), các bài khó còn dở dang hoặc rất sơ sài (DBA-H-13, DBA-H-14), và bài 'sản phẩm dưới 20.000 hoặc chưa bán' dùng INNER JOIN nên logic sai (cần LEFT JOIN). Mức độ hoàn chỉnh: có 1 câu bỏ trống, 1 câu chỉ là đoạn truy vấn dở dang chưa chạy được, vài câu chờ mức cơ bản. Hướng cải thiện: viết truy vấn hoàn chỉnh end-to-end, dùng đúng tên cột và bảng theo đúng schema đã cho, trình bày Stored Procedure đầy đủ với phần in số bản ghi, và tránh bỏ trống câu hỏi. |
| 13 | TuyenLQ4 | 4.00 | 48.0/120 | 10/12 | Kiến thức tốt: thành thạo SQL truy vấn cơ bản (SELECT, DISTINCT, LIKE N'' cho chuỗi Unicode), nắm được khái niệm khóa phức hợp, biết thiết kế bảng với khóa chính/khóa ngoại và chèn dữ liệu. Kiến thức cần cải thiện: khái niệm Alias và Trigger còn bỏ trống/sai; chưa dùng được CTE; kiểu dữ liệu thời điểm (DATE vs DATETIME); cú pháp SQL chưa chuẩn (VALUES, ngoặc, hàm DATEDIFF, @@ROWCOUNT); thiếu kỹ thuật GROUP BY/HAVING để so sánh chi tiết với giá trị tổng hợp. Mức độ hoàn chỉnh: 10/~12 câu có nội dung; các câu Easy/Medium truy vấn làm tốt, các câu Hard thiết kế hệ thống (CTE, schema nâng cao, nhiều bảng) hoàn thành dưới 50%, 2 câu bỏ trống. Hướng cải thiện: luyện WITH CTE cùng GROUP BY/HAVING và UNION cho bài toán so sánh giá trị đơn với tổng hợp; ôn khái niệm Alias, Trigger, STORED PROCEDURE chuẩn cú pháp SQL Server (DATEDIFF, @@ROWCOUNT, NVARCHAR/MONEY); rèn cú pháp CREATE TABLE đầy đủ ngoặc và kiểu dữ liệu phù hợp; tránh để trống câu hỏi khó. |
| 14 | BaoVH4 | 3.42 | 41.0/120 | 11/12 | Kiến thức tốt: nắm được khái niệm CSDL, xác định đúng các khóa chính của lược đồ Quản lý Bán hàng, và viết được truy vấn lọc cơ bản với WHERE (Age > 50). Kiến thức cần cải thiện: cú pháp SQL còn nhiều lỗi trầm trọng (thiếu FROM, điều kiện join sai, viết thường/sai tên bảng - cột), chưa thành thạo GROUP BY - HAVING để đếm và tìm bản ghi trùng, chưa biết dùng DISTINCT và hàm LEN, chưa viết được Stored Procedure. Mức độ hoàn chỉnh: thấp — 11/12 câu có trả lời nhưng hầu hết dở dang, sai hoặc không thực thi được, 1 câu bỏ trống (row 13). Hướng cải thiện: nắm chắc thứ tự mệnh đề SELECT - FROM - JOIN - WHERE - GROUP BY - HAVING; viết hoàn chỉnh từng câu lệnh rồi tự rà soát trước khi nộp; luyện viết View/Stored Procedure cơ bản và các hàm xử lý chuỗi (LEN, DISTINCT, UPPER); tránh để câu trả lời dở dang. |
| 15 | HuyenNTK43 | 2.75 | 33.0/120 | 7/12 | Kiến thức tốt: sinh viên nắm ý nghĩa cơ bản của DELETE/TRUNCATE/DROP, SELECT *, tự làm được GROUP BY và JOIN theo điều kiện khóa. Kiến thức cần cải thiện: cú pháp SQL Server (IDENTITY, DROP COLUMN), tạo bảng đủ ràng buộc và đủ bảng, Self-Join, Trigger, Stored Procedure. Mức độ hoàn chỉnh thấp: 5/12 câu bỏ trống (row 4, 10, 11, 12, 13), nhiều bài bị thiếu bảng/cột hoặc sai cú pháp. Hướng cải thiện: luyện viết cú pháp chuẩn từng lệnh, đọc kỹ yêu cầu để chọn đúng cột và JOIN đủ bảng, không bỏ trống câu hỏi dù chỉ nêu ý tưởng. |
| 16 | KhanhHQ15 | 0.33 | 4.0/120 | 1/12 | Kiến thức tốt: học viên có nhận biết cơ bản về subquery và liên kết MgrNo → EmpNo ở câu 2. Kiến thức cần cải thiện: thiếu sót rất lớn — 11/12 câu bỏ trống, không trả lời SQL, khái niệm và phân tích khóa chính/khóa ngoại. Mức độ hoàn chỉnh: gần như không hoàn thành bài; duy nhất câu 2 đạt một phần nhưng còn lỗi cú pháp và thiếu cột email. Hướng cải thiện: cần làm bài đầy đủ, luyện lại Self-Join, các truy vấn SELECT/JOIN cơ bản, DDL có ràng buộc và phân biệt PK/FK trước khi thi lại. |
